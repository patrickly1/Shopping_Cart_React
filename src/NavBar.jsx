import React from "react";

const NavBar = ({ Cart }) => {
    return (
        <>
            <div>This is the Nav Bar</div>
            <div>Cart ({Cart})</div>
        </>
    );
};

export default NavBar;
